# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.

specVersion: 0.0.2
description: Unlock Protocol & Associated contracts
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: Unlock
    network: {{network}}
    source:
      address: "{{unlockUnlockAddress}}"
      abi: Unlock
      startBlock: {{startBlock}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - NewLock
        - NewTokenURI
        - NewGlobalTokenSymbol
        - OwnershipTransferred
      abis:
        - name: Unlock
          file: ./abis/Unlock.json
        - name: PublicLock
          file: ./abis/PublicLock.json
      eventHandlers:
        - event: NewLock(indexed address,indexed address)
          handler: handleNewLock
        - event: NewTokenURI(string)
          handler: handleNewTokenURI
        - event: NewGlobalTokenSymbol(string)
          handler: handleNewGlobalTokenSymbol
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred
      file: ./src/mapping.ts
  {{#previousDeploys}}
  - kind: ethereum/contract
    name: Unlock{{index}}
    network: {{network}}
    source:
      address: "{{unlockAddress}}"
      abi: Unlock{{index}}
      startBlock: {{startBlock}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - NewLock
        - NewTokenURI
        - NewGlobalTokenSymbol
        - OwnershipTransferred
      abis:
        - name: Unlock{{index}}
          file: ./abis/Unlock.json
        - name: PublicLock
          file: ./abis/PublicLock.json
      eventHandlers:
        - event: NewLock(indexed address,indexed address)
          handler: handleNewLock
        - event: NewTokenURI(string)
          handler: handleNewTokenURI
        - event: NewGlobalTokenSymbol(string)
          handler: handleNewGlobalTokenSymbol
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred
      file: ./src/mapping.ts
  {{/previousDeploys}}

templates:
  - kind: ethereum/contract
    name: PublicLock
    network: {{network}}
    source:
      abi: PublicLock
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - PublicLock
        - KeyHolder
        - KeyPurchase
      abis:
        - name: PublicLock
          file: ./abis/PublicLock.json
      eventHandlers:
        - event: Transfer(indexed address,indexed address,indexed uint256)
          handler: handleTransfer
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleLockTransfer
        - event: PriceChanged(uint256,uint256)
          handler: handlePriceChanged
        - event: CancelKey(indexed uint256,indexed address,indexed address,uint256)
          handler: handleCancelKey
        - event: ExpireKey(uint256)
          handler: handleExpireKey
      file: ./src/prev7Mappings/mapping.ts

  - kind: ethereum/contract
    name: PublicLock7
    network: {{network}}
    source:
      abi: PublicLock
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - PublicLock7
      abis:
        - name: PublicLock
          file: ./abis/PublicLock7.json
      eventHandlers:
        - event: CancelKey(indexed uint256,indexed address,indexed address,uint256)
          handler: cancelKey
        - event: ExpirationChanged(indexed uint256,uint256,bool)
          handler: expirationChanged
        - event: ExpireKey(indexed uint256)
          handler: expireKey
        - event: LockManagerAdded(indexed address)
          handler: lockManagerAdded
        - event: LockManagerRemoved(indexed address)
          handler: lockManagerRemoved
        - event: PricingChanged(uint256,uint256,address,address)
          handler: pricingChanged
        - event: Transfer(indexed address,indexed address,indexed uint256)
          handler: transfer
        - event: RenewKeyPurchase(indexed address,uint256)
          handler: renewKeyPurchase
      file: ./src/v7Mappings/mapping.ts

  - kind: ethereum/contract
    name: PublicLock10
    network: {{network}}
    source:
      abi: PublicLock
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - PublicLock10
      abis:
        - name: PublicLock
          file: ./abis/PublicLockV10.json
      eventHandlers:
        - event: CancelKey(indexed uint256,indexed address,indexed address,uint256)
          handler: cancelKey
        - event: ExpirationChanged(indexed uint256,uint256,bool)
          handler: expirationChanged
        - event: ExpireKey(indexed uint256)
          handler: expireKey
        - event: LockManagerAdded(indexed address)
          handler: lockManagerAdded
        - event: LockManagerRemoved(indexed address)
          handler: lockManagerRemoved
        - event: PricingChanged(uint256,uint256,address,address)
          handler: pricingChanged
        - event: Transfer(indexed address,indexed address,indexed uint256)
          handler: transfer
        - event: KeyExtended(indexed uint256,uint256)
          handler: extendKey


      file: ./src/v10Mappings/mapping.ts

