import React from 'react'
import PaywallContent from '../components/content/PaywallContent'

// TODO: remove since we are using the paywall app separately
const Paywall = () => <PaywallContent />

export default Paywall
