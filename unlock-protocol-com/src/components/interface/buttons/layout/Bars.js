import React from 'react'
import Svg from '../../svg'
import LayoutButton from '../LayoutButton'

const Bars = (props) => (
  <LayoutButton {...props}>
    <Svg.Bars title="Bars" />
  </LayoutButton>
)

export default Bars
