import React from 'react'
import LayoutButton from '../LayoutButton'

const Blog = (props) => (
  <LayoutButton href="/blog" label="Blog" {...props}>
    Blog
  </LayoutButton>
)

export default Blog
