export const unlockConfig = {
  gaId: process.env.NEXT_PUBLIC_GA_ID,
  gaTmId: process.env.NEXT_PUBLIC_GA_TM_ID,
  baseURL: process.env.NEXT_PUBLIC_URL_BASE,
  appURL: process.env.NEXT_PUBLIC_UNLOCK_APP_URL,
}
