{
  "name": "unlock",
  "version": "1.0.0",
  "description": "Unlock is a protocol for decentralized access control",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "dependencies": {
    "eslint-plugin-jest": "^21.22.1"
  },
  "devDependencies": {
    "husky": "^1.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-deps": "cd smart-contracts && npm install && cd ../unlock-app && npm install",
    "start-ganache": "ganache-cli -b 3 -i 1984 --mnemonic 'hello unlock save the web'",
    "deploy-lock": "cd smart-contracts && truffle deploy --reset"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unlock-protocol/unlock.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/unlock-protocol/unlock/issues"
  },
  "homepage": "https://github.com/unlock-protocol/unlock#readme"
}
