{
  "name": "provider",
  "devDependencies": {
    "@cloudflare/workers-types": "4.20241127.0",
    "@types/node": "22.9.0",
    "@unlock-protocol/networks": "workspace:^",
    "typescript": "5.6.3",
    "vitest": "2.1.4"
  },
  "dependencies": {
    "ethers": "6.13.4",
    "wrangler": "3.84.1"
  },
  "scripts": {
    "dev": "yarn wrangler dev",
    "deploy": "yarn wrangler deploy",
    "set-provider-urls": "op run --env-file=.op.env --  ./scripts/set-provider-urls.sh",
    "test": "vitest"
  }
}
