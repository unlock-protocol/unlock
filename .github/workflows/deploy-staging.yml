name: Staging Deployment
on: 
  push:
    branches:
      - master
jobs:
  deploy-locksmith-staging:    
    uses: unlock-protocol/unlock/.github/workflows/heroku.yml@gh-actions
    with:
      service: locksmith
      app-name: unlock-locksmith-staging
