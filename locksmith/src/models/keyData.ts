interface KeyData {
  token: string
  lockName: string
  expiration: string
  keyholderAddress: string
  keyManager: string
  lockAddress: string
  transactionsHash: string[]
  email: string
}

export default KeyData
