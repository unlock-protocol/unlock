import React from 'react'
import Paywall from '../paywall/Paywall'

// note: eventually we will eliminate the old PaywallContent and replace
// it with this one
export default function Paywall2Content() {
  return <Paywall />
}
