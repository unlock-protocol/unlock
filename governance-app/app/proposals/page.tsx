import { RoutePlaceholder } from '~/components/layout/RoutePlaceholder'

export default function ProposalsPage() {
  return (
    <RoutePlaceholder
      title="Proposals"
      description="Proposal list shell with the route, navigation, and placeholder state in place for the read-path implementation slice."
    />
  )
}
