import { RoutePlaceholder } from '~/components/layout/RoutePlaceholder'

export default function ProposePage() {
  return (
    <RoutePlaceholder
      title="Create Proposal"
      description="Creation flow shell with future support for known-contract proposal building and advanced JSON mode."
    />
  )
}
