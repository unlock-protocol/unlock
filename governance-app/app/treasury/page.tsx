import { RoutePlaceholder } from '~/components/layout/RoutePlaceholder'

export default function TreasuryPage() {
  return (
    <RoutePlaceholder
      title="Treasury"
      description="Treasury page shell with the route in place for RPC-based ETH and token balance reads."
    />
  )
}
